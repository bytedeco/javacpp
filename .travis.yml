dist: xenial
sudo: required
language: java
install: true
addons:
    apt:
      packages: maven


jobs:
  include:
    - os: linux
      arch: arm64
      script: "mvn install -B -U -s $HOME/settings.xml"
