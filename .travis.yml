dist: xenial
sudo: required
language: java
install: true
addons:
    apt:
      packages: maven


jobs:
  include:
    - os: linux
      arch: arm64
      script: "sudo apt install -y openjdk-8-jdk-headless; mvn install -B -U"
